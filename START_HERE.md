# 🚀 Start Definitions Manager

## Quick Start

**Double-click `app.py` to start the app!**

Or from command line:
```bash
cd "D:\THEOPHYSICS_MASTER\Apps\Obsidian-Definitions-Manager"
python app.py
```

## Perfect Location! ✨

✅ **Inside Your Vault** - `THEOPHYSICS_MASTER/Apps/Obsidian-Definitions-Manager/`  
✅ **Auto-Detects Vault** - Knows it's in THEOPHYSICS_MASTER automatically  
✅ **Syncs with Vault** - If vault syncs, app syncs too  
✅ **Always Findable** - Right here in your vault structure  
✅ **Portable** - Move vault, app comes with it  
✅ **Still Standalone** - Runs independently, not an Obsidian plugin  

## Features

- 📚 Manage all definitions in one clean GUI
- ✏️ Add, edit, delete definitions easily  
- 🔍 Browse and search definitions
- 🔗 Works with Obsidian Note Definitions plugin
- 🎨 Modern dark theme interface (like Stratum)
- 🤖 Ready for AI integration

## Integration

The app automatically detects it's inside `THEOPHYSICS_MASTER` and uses the correct paths.

Both the app and the Obsidian plugin read/write the same definition files, so they stay in sync!

## Create a Shortcut

1. Right-click `app.py`
2. Create shortcut
3. Move shortcut to desktop or taskbar
4. Double-click to start anytime!

## First Time Setup

1. Install dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Run the app:
   ```bash
   python app.py
   ```

3. The app will auto-detect your vault location!
